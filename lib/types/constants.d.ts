export declare const defaultSocketOptions: {
    secure: boolean;
    hostname: string;
    port: number;
    autoReconnect: boolean;
    autoReconnectOptions: {
        randomness: number;
    };
};
